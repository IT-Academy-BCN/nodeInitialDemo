/*const getTime = (req,res) => {
    try {
    res.status(200).send({
         date: new Date().toLocaleString()
        })
    } catch (err) {
    res.status(500).send({
        status: "Error",
        message: "Servidor no encontrado. // No s'ha trobat el servidor. //Server doom!"
    });
  } 
}

module.exports = getTime;*/